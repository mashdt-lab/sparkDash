/** Reserved tab id for the cross-Spark overview (not a real Spark). */
export const OVERVIEW_ID = "__overview__";
/** Reserved tab id for the Services launcher (not a real Spark). */
export const SERVICES_ID = "__services__";
/** Reserved tab id for the embedded GPU Dashboard comparison view (not a real Spark). */
export const GPU_COMPARE_ID = "__gpu_compare__";
